# Projeto de Testes Automatizados com Cypress e Cucumber

Este é um projeto de testes automatizados utilizando **Cypress** e **Cucumber** para facilitar a escrita de testes em uma linguagem mais legível. O projeto é configurado para rodar testes de interface e API.

## Tecnologias Utilizadas

- **Node.js**: Ambiente de execução JavaScript no lado do servidor.
- **npm**: Gerenciador de pacotes para instalar dependências.
- **Cypress**: Ferramenta de teste end-to-end para aplicações web.
- **Cucumber**: Ferramenta que suporta o desenvolvimento orientado a comportamento (BDD).
- **cypress-cucumber-preprocessor**: Integra o Cucumber com o Cypress para testes em formato Gherkin.

# Pré-requisitos

- Node.js e npm instalados na sua máquina. Você pode baixar o Node.js [aqui](https://nodejs.org/).


## Instalação

1. Clone o repositório:

   ```bash
   git clone https://github.com/Brunolima96/Advantage-Api-Web.git


2. Instale as dependências:
    npm install

3. Executando os Testes
    
    Para abrir a interface do Cypress: npx cypress open

